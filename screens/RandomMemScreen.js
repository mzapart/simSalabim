import React from "react";
import { Text, View } from "react-native";

class RandomMemScreen extends React.Component {
  render() {
    return (
      <View>
        <Text>Every1 can see it fetch random Mem</Text>
      </View>
    );
  }
}

export default RandomMemScreen;
